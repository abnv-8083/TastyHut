{
    "name": "tastyhut",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "client",
        "server"
    ],
    "scripts": {
        "dev": "npm run dev --workspace=client & npm run start --workspace=server",
        "install-all": "npm install --workspace=client && npm install --workspace=server"
    }
}